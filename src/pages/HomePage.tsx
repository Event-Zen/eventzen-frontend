import HomeCarousel from "../components/HomeCarousel";

const HomePage = () => {
  return (
    <div>
      <HomeCarousel />
      {/* next sections here */}
    </div>
  );
};

export default HomePage;